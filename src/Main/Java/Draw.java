package Main.Java;

public class Draw {

    public static boolean isFull(int[] array) {
        // Check if the array is null or empty
        if (array == null) {
            return false;
        }

        // Compare the array's length to the number of elements
        return array.length == array.length;
    }

    public String draw(){


        String flag = "pending";

        String [] mat = {"x","o","x","o","x","o","x","o","x"};
        if(isFull(mat)){

        }
        while ((mat)

    }
}
